Stage all changes with `git add .` and commit them with the commit message from the arguments: $ARGUMENTS

If the arguments contain `-p`, also push to the remote after committing. Remove `-p` from the commit message.